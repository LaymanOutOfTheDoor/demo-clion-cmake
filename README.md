### 编译方式

* linux
  * 进入编译目录
    ```shell
    $ cd build
    ```
  * Release
    ```shell
    $ cmake ..
    $ make
    ```
  * run
    ```shell
    $ ./Interview
    ```
* windows
  * 安装cmake
  * 使用clion打开项目